# Prod-local hardening for observability UIs: keep Jaeger/Prometheus/Grafana internal.
# Use with docker-compose.yml + compose.obs.yml + profile observability + compose.prod.yml.
services:
  jaeger:
    ports: !reset []
    networks:
      - private

  prometheus:
    ports: !reset []
    networks:
      - private

  grafana:
    ports: !reset []
    networks:
      - private

  otel-collector:
    ports: !reset []
    networks:
      - private
