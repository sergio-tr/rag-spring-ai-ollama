# OpenTelemetry Collector (contrib). Base image from observability/.env (OTEL_COLLECTOR_BASE_IMAGE).
ARG BASE_IMAGE=otel/opentelemetry-collector-contrib:0.102.0
FROM ${BASE_IMAGE}
# Config and command are set in compose.
EXPOSE 4317 4318 8889
