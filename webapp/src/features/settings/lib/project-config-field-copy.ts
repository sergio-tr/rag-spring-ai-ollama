/**
 * Human-readable labels for known project RAG config schema keys (GET /config/schema).
 * @deprecated Use {@link labelConfigField} from `config-field-copy`.
 */
export { labelConfigField as labelProjectConfigField } from "@/features/settings/lib/config-field-copy";
