# Benchmark scenarios (`tests/performance/scenarios/`)

YAML scenarios for `tests/performance/retrieval_benchmark.py` and related runners. Current release scenarios use `transport: product_chat` with prompts and project-scoped toggles; historical transports are for explicitly labelled comparisons only.

See [../README.md](../README.md) for commands, env vars (`BENCHMARK_*`), and report schema.
