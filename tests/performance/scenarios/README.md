# Benchmark scenarios (`tests/performance/scenarios/`)

YAML scenarios for `tests/performance/retrieval_benchmark.py` and related runners. Each file defines transport (`legacy` vs `product_chat`), prompts, and toggles for retrieval and tools.

See [../README.md](../README.md) for commands, env vars (`BENCHMARK_*`), and report schema.
