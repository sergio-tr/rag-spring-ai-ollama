name: reasoning_on
description: reasoningEnabled=true - requires product_chat + BENCHMARK_* env.
transport: product_chat
product_chat:
  product_base_path: /api/v5
  rag_config:
    reasoningEnabled: true
  post_message: {}
