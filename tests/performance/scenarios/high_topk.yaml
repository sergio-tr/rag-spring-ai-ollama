# Requires product_chat + BENCHMARK_* env — applies project RAG config before chat.
name: high_topk
description: Project-scoped topK=25 (compare with low top-k baseline).
transport: product_chat
product_chat:
  product_base_path: /api/v5
  rag_config:
    topK: 25
  post_message: {}
