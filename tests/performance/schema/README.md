# Benchmark report schema (`tests/performance/schema/`)

JSON Schema for micro-benchmark output (`schemaVersion` in reports). Used by `tests/performance/retrieval_benchmark.py` and documented in [../API_RESPONSE_AUDIT.md](../API_RESPONSE_AUDIT.md).

See [../README.md](../README.md) for how reports are produced.
