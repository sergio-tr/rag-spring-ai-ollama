# Optional pricing table for rough USD estimates (not billing-grade).
# Reference in: python retrieval_benchmark.py --pricing-yaml tests/performance/pricing.example.yaml
#
# Keys under models match chatModel / llmModel strings when provided.
models:
  llama3.2:
    per1kTokensUsd: 0.0
  mistral:
    inputPer1kUsd: 0.0
    outputPer1kUsd: 0.0
