# Canonical Postgres image for GitHub Actions job services (pgvector).
# Must match POSTGRES_BASE_IMAGE default in db/.env.example.
# Referenced by .github/workflows/reusable-ci-core.yml (env POSTGRES_SERVICE_IMAGE) and verified by .github/scripts/verify-pinned-postgres-image.sh.
POSTGRES_SERVICE_IMAGE=pgvector/pgvector:0.8.2-pg16-bookworm
