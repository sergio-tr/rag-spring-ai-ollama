package com.uniovi.rag.domain.runtime.traceregressionsuiterun;

/** Per-entry execution row shape discriminator (P41). */
public enum RuntimeTraceRegressionSuiteRunPersistedExecutionStatus {
    BATCH_RETURNED,
    EXECUTION_FAILED
}
