package com.uniovi.rag.domain.runtime.traceregressionsuiterun;

import java.util.UUID;

/** Stable identifier for a persisted regression suite run (P41). */
public record RuntimeTraceRegressionSuiteRunId(UUID value) {}
