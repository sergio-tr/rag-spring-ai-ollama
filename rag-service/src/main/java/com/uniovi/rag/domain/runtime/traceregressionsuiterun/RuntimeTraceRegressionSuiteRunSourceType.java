package com.uniovi.rag.domain.runtime.traceregressionsuiterun;

/** How a suite run was initiated for persistence (P41). */
public enum RuntimeTraceRegressionSuiteRunSourceType {
    AD_HOC,
    SAVED_DEFINITION
}
