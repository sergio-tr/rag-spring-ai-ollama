package com.uniovi.rag.application.service.runtime.retrieval;

/**
 * Prompt layout for curated context text (matches prior dense workflow layouts).
 */
public enum RetrievalLayout {
    DOCUMENT_COMBINED,
    CHUNK_SEPARATE
}
