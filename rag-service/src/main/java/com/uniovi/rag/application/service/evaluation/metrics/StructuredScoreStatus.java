package com.uniovi.rag.application.service.evaluation.metrics;

/** Whether a deterministic structured score was computed for an item. */
public enum StructuredScoreStatus {
    COMPUTED,
    NOT_AVAILABLE
}
