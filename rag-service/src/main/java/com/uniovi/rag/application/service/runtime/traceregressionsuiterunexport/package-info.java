/**
 * ZIP export adapter for persisted regression suite runs.
 * Loads persisted runs only; no orchestration beans on controller paths.
 */
package com.uniovi.rag.application.service.runtime.traceregressionsuiterunexport;
