# Backend package map (`rag-service`)

**Navigation only** — not a substitute for [rag-service/README.md](../../rag-service/README.md) (build, profiles, smoke tests). One-line roles for `com.uniovi.rag.*`. For methods, use **Javadoc**: `cd rag-service && ./mvnw javadoc:javadoc` → `target/site/apidocs`.

| Package / area | Role |
|----------------|------|
| `application.port` | Hexagonal ports: `ConfigurationSourcePort` (layer loads + raw preset/profile sources), `ConversationRuntimeOverrideLoader`, `RagConfigurationResolver` |
| `application.port.out` | Outbound ports (interfaces) for use cases |
| `application.config` | Runtime configuration resolution: `ConfigResolverService`, `RuntimeConfigResolutionInput`, `CompatibilityValidator`, `ReindexImpactAnalyzer`, `SystemPromptComposer` |
| `application.service` | Use cases including `ResolvedConfigSnapshotApplicationService` (persist resolved snapshots after `ConfigResolverService.resolve`) |
| `application.service.knowledge` | Knowledge System: `KnowledgeConfigurationIntegrationService` (config-aware preview/execute orchestration), `KnowledgeBuildProjectionMapper` (sole builder of `KnowledgeBuildProjection`), `KnowledgePipelineOrchestrator` (sole corpus `document_artifact` + `vector_store` writes; config-aware rebuild takes `KnowledgeBuildProjection`), `KnowledgeIndexingService`, `KnowledgeIngestionService` (persists default `resolved_config_snapshot` per ingest for linkage), `KnowledgeSnapshotService`, `ReindexService` (executes precomputed `KnowledgeReindexDecision` only), `ProjectKnowledgeApplicationService` (REST façade → integration service; no direct resolver/orchestrator) |
| `application.usecase` | Application services / use cases (e.g. auth) |
| `api.auth` | REST auth controllers, DTOs, exceptions (not application logic) |
| `api.admin` | `ROLE_ADMIN`: allowlist, Ollama pull orchestration |
| `interfaces.rest` | Product REST: projects, documents, conversations, knowledge (`ProjectKnowledgeController`: ingest, rebuild preview/execute, legacy reindex alias, snapshots), SSE messages, presets, lab jobs, config schema, **P20** replay-comparison GETs (`RuntimeTraceReplayComparisonController` → `RuntimeTraceReplayComparisonService` only), **P22** standalone replay JSON GETs (`RuntimeTraceReplayController` → `RuntimeTraceReplayService` only), **P23** standalone replay ZIP GETs (`RuntimeTraceReplayExportController` → `RuntimeTraceReplayExportService` only; base path `rag.api.product-base-path`), **P34**/**P35**/**P36**/**P47**/**P50**/**P52**/**P53**/**P54**/**P55** suite-definition routes on **`RuntimeTraceRegressionSuiteDefinitionController`** (**P56:** ArchUnit + WebMvc lock **global** vs **definition-scoped** run/ZIP surfaces — **`RuntimeTraceRegressionSuiteRunController`** **without** run ZIP **`@Service`** deps; **P43**/**P44**/**P45** controllers frozen single-handler paths; **no** **`ForDefinition`** ZIP calls from global controllers; definition controller path families frozen under **`…/runtime-trace-regression-suite-definitions/…`**. **P57:** **global definition-document ZIP** (**P38**/**P39**/**P40**) on **`RuntimeTraceRegressionSuiteDefinitionExportController`** / **`DefinitionImportController`** / **`DefinitionImportPreviewController`** only — **no** run-layer **`@Service`** on those controllers; **`RuntimeTraceRegressionSuiteDefinitionController`** **must not** duplicate **`…/{definitionId}/export`**, **`…/import`**, **`…/import/preview`** without **`/runs/`** or depend on definition ZIP **`@Service`** beans — **P53**/**P54**/**P55** use **`…/runs/...`**) **P58:** **ArchUnit** + **`T-P58-svc-zip`** lock **`RuntimeTraceRegressionSuiteDefinitionExportService`**, **`RuntimeTraceRegressionSuiteDefinitionImportService`**, **`RuntimeTraceRegressionSuiteDefinitionImportPreviewService`** — **no** ctor/field deps and **no** method-body accesses targeting **`RuntimeTraceRegressionSuiteRunExportService`**, **`RuntimeTraceRegressionSuiteRunImportService`**, **`RuntimeTraceRegressionSuiteRunImportPreviewService`**, or **`RuntimeTraceRegressionSuiteRunPersistenceService`**; **P47**–**P55** on **`RuntimeTraceRegressionSuiteDefinitionController`** stay on run **`@Service`** beans only (see **P58** in [rag-runtime-architecture.md](rag-runtime-architecture.md)). **P59:** **`p59-runtime-trace-regression-suite-http-inventory.md`** + P59 plan matrix, **`RuntimeTraceRegressionSuiteHttpMappingInventoryTest`** (**T-P59-collision**, **T-P59-handler-count**, **T-P59-prefix-ownership**, **T-P59-controller-beans**), **`RuntimeTraceRegressionSuiteP59MappingStyleArchitectureTest`** (**T-P59-mapping-style**); see **P59** in [rag-runtime-architecture.md](rag-runtime-architecture.md). (**P47:** **`POST …/{definitionId}/runs`** and conversation **`POST …/runs`** — materialize + **`execute`** + **`createRun`** **`SAVED_DEFINITION`**, **no** orchestration **`@Service`**; **P50:** **`GET …/{definitionId}/runs`** and **`GET …/{definitionId}/runs/{runId}`** — **`loadByIdForUser`** gate + **`listSummariesForUserAndDefinition`** / **`loadByIdForUserAndDefinition`** only, **no** **`execute`**/**`createRun`**; **P52:** **`DELETE …/{definitionId}/runs/{runId}`** — same gate + **`deleteRunForUserAndDefinition`** only, **no** **`deleteRunForUser`** two-arg; **P53:** **`GET …/{definitionId}/runs/{runId}/export`** — gate + **`exportRunZipForDefinition`** on **`RuntimeTraceRegressionSuiteRunExportService`** only, **no** controller **`runPersistenceService`**; **P54:** **`POST …/{definitionId}/runs/import`** — gate **before** body + **`importRunZipForDefinition`** on **`RuntimeTraceRegressionSuiteRunImportService`** only, **no** controller **`runPersistenceService`**; **P55:** **`POST …/{definitionId}/runs/import/preview`** — gate **before** body + **`previewImportZipForDefinition`** on **`RuntimeTraceRegressionSuiteRunImportPreviewService`** only, **no** controller **`runPersistenceService`**/**`runImportService`**), **P37** saved-definition execution ZIP **`POST`** on **`RuntimeTraceRegressionSuiteDefinitionExecutionExportController`** → **`RuntimeTraceRegressionSuiteDefinitionExecutionExportService`** only, **P38** saved-definition ZIP **`GET …/{definitionId}/export`** on **`RuntimeTraceRegressionSuiteDefinitionExportController`** → **`RuntimeTraceRegressionSuiteDefinitionExportService`** only, **P39** P38-strict definition ZIP **`POST …/import`** on **`RuntimeTraceRegressionSuiteDefinitionImportController`** → **`RuntimeTraceRegressionSuiteDefinitionImportService`** only, **P40** P38-strict ZIP **`POST …/import/preview`** on **`RuntimeTraceRegressionSuiteDefinitionImportPreviewController`** → **`RuntimeTraceRegressionSuiteDefinitionImportPreviewService`** only, **P42** persisted suite-run **`GET`** list/detail on **`RuntimeTraceRegressionSuiteRunController` → `RuntimeTraceRegressionSuiteRunPersistenceService` only**, **P46** ad hoc execute-then-persist suite-run **`POST`** (explicit + conversation-scoped) on **`RuntimeTraceRegressionSuiteRunController`** → **`RuntimeTraceRegressionSuiteService#execute`** then **`RuntimeTraceRegressionSuiteRunPersistenceService#createRun`** only (**`RuntimeTraceRegressionSuiteHttpAdapter`** + strict mapper; **no** orchestration **`@Service`**), **P49** **`DELETE …/{runId}`** on **`RuntimeTraceRegressionSuiteRunController` → `RuntimeTraceRegressionSuiteRunPersistenceService#deleteRunForUser` only** (**no** **`execute`**, **`createRun`**, **`loadByIdForUser`**, **`listSummariesForUser`**), **P43** suite-run ZIP **`GET …/{runId}/export`** on **`RuntimeTraceRegressionSuiteRunExportController` → `RuntimeTraceRegressionSuiteRunExportService` only**, **P44** P43-strict suite-run ZIP **`POST …/runtime-trace-regression-suite-runs/import`** on **`RuntimeTraceRegressionSuiteRunImportController` → `RuntimeTraceRegressionSuiteRunImportService` only**, **P45** P43-strict suite-run ZIP **`POST …/runtime-trace-regression-suite-runs/import/preview`** on **`RuntimeTraceRegressionSuiteRunImportPreviewController` → `RuntimeTraceRegressionSuiteRunImportPreviewService` only** |
| `interfaces.rest.dto.knowledge` | Snapshot responses; `KnowledgeIngestRequest` / `KnowledgeReindexRequest` (query-parameter shapes for knowledge routes) |
| `controller` | Legacy RAG HTTP surface (`RagController`, Ollama helpers) — prefix from `rag.api.legacy-base-path` |
| `configuration` | Spring Security, CORS, feature flags, path properties (`RagApiPathProperties`), beans wiring; **P35** **`RegressionSuiteDefinitionMutationJacksonConfiguration`** (**`definitionMutationStrictObjectMapper`**) for suite-definition mutation JSON |
| `security` | JWT filter, `JwtService`, `RagPrincipal` |
| `bootstrap` | Startup seeders (e.g. e2e admin), safety validators |
| `domain` | Domain enums and types (framework-free top-level package) |
| `domain.knowledge` | Knowledge System domain: `WorkspaceDocument`, `MaterializationStrategy`, artifact types, snapshot scope, snapshot/reindex hashes (no JPA in this package) |
| `domain.config.capability` | `Capability`, `CapabilitySet`, `CapabilityGroup` (activation / presence for resolution) |
| `domain.config.rules` | Declarative `CompatibilityRule` implementations (evaluated by `CompatibilityValidator`) |
| `domain.config.runtime` | `ResolvedRuntimeConfig`, `ResolvedConfigSnapshot`, provenance / profile types for resolution |
| `domain.config` (merge helpers) | `RagConfigurationMerge`, `PresetProfilePayloadMerge` (pure JSON/map merge; **invoked only from** `ConfigResolver` in production) |
| `domain.config.indexing` | `ReindexImpact` / `ReindexImpactLevel` (semantic reindex preview) |
| `domain.config.prompt` | `SystemPromptLayers` and related prompt-layer types (composition in `SystemPromptComposer`) |
| `domain.runtime` | Effective RAG config / feature snapshots used during a query |
| `infrastructure.persistence` | Spring Data JPA repositories, `ConversationRuntimeOverrideLoaderImpl`, custom persistence adapters |
| `infrastructure.persistence.traceregressionsuitedefinition` | **P33:** JPA entities + repositories + mapper for **`runtime_trace_regression_suite_*`** tables only (consumed exclusively by **`RuntimeTraceRegressionSuiteDefinitionService`**). |
| `infrastructure.persistence.traceregressionsuiterun` | **P41:** JPA entities + repositories + mapper for **`runtime_trace_regression_suite_run`** and **`runtime_trace_regression_suite_run_entry`** (consumed exclusively by **`RuntimeTraceRegressionSuiteRunPersistenceService`**). **P48:** derived delete **`deleteByIdAndUserId`** on the run repository only; entry rows cascade via **`V33`** FK (**no** entry-repo deletes from application code). **P50:** **`findAllByUserIdAndDefinitionIdOrderByCreatedAtDescIdAsc`**, **`findByIdAndUserIdAndDefinitionId`** for definition-scoped run reads. **P51:** derived delete **`deleteByIdAndUserIdAndDefinitionId`** (definition-scoped delete). |
| `infrastructure.persistence.jpa` | JPA entities and entity factories |
| `infrastructure.persistence.mapper` | `ResolvedConfigSnapshotEntityMapper` (sole read/write shape for `resolved_config_snapshot` JSON columns); `KnowledgeIndexSnapshotMapper`, `ReindexEventMapper` (knowledge domain ↔ JPA) |
| `infrastructure.classifier` | HTTP clients to **classifier-service** (`ClassifierLabClient`, `QueryClassifier`, etc.) |
| `service.query` | `ProcessQueryService` / `SimpleProcessQueryService` façade → `application.service.runtime` (`ExecutionContextFactory`, `RagExecutionOrchestrator`, workflows) |
| `application.service.runtime` | Runtime engine: orchestrator, workflow selector, full-corpus assembly |
| `application.service.runtime.advisor` | P10 Advisor Core: `AdvisorPolicyResolver`, `AdvisorStrategy`, `RetrievalAdvisor`, `ContextPackingAdvisor` (orchestrated only from `RagExecutionOrchestrator`) |
| `application.service.runtime.retrieval` | Advanced retrieval: `AdvancedRetrievalPipeline` (single entrypoint), dense/sparse/hybrid strategies, RRF fusion, deterministic rerank/filter/compression, metadata appendix loader |
| `application.service.runtime.query` | Runtime query understanding: `QueryUnderstandingPipeline` and adapters/resolvers producing `QueryPlan` |
| `application.service.runtime.clarification` | **P11:** `ClarificationStateResolver`, `ClarifiedQueryRefiner`, `ClarificationPolicyResolver`, `ClarificationQuestionGenerator`, `ClarificationStrategy` (sole mutator caller for `PendingClarificationStore`); deterministic policy + templates; runs after QU, before `WorkflowSelector` |
| `application.service.runtime.memory` | **P12:** runtime-owned conversational memory stage: history loader, fixed slice selector, LLM-backed single-call condensation with deterministic fallback; runs after P11 clarification pre-processing and before QU |
| `application.service.runtime.routing` | **P13:** adaptive routing stage: deterministic route-family selection (`AdaptiveRoutingPolicyResolver`) + single entrypoint (`AdaptiveRoutingStrategy`) + capability evaluator + gate builder; runs after clarification policy and before any downstream execution family |
| `application.service.runtime.judge` | **P14:** post-answer judge stage: bounded evaluation + at most one bounded repair attempt; orchestrated only from `RagExecutionOrchestrator` after the selected route family produces a candidate answer |
| `application.service.runtime.tracepersistence` | **P15:** runtime trace persistence (best-effort). Writes one canonical `runtime_execution_trace` row per completed orchestrated turn by consuming the finalized `ExecutionTrace` (no re-derivation). Invoked at the runtime boundary (facade), not from controllers or workflows. |
| `application.service.runtime.tracequery` | **P16:** runtime trace query surfaces (read-only). `RuntimeTraceQueryService` is the single owner for reading the canonical persisted trace artifact (`runtime_execution_trace`) and applying owner-scoped authorization + paging/filter rules. REST controllers must not query repositories directly. |
| `application.service.runtime.traceexport` | **P17:** runtime trace export core (read-only). `RuntimeTraceExportService` is the single owner for generating bounded, deterministic ZIP exports of persisted runtime traces by consuming P16 query surfaces only (no repository bypass, no DB writes, no replay/analytics). |
| `application.service.runtime.tracereplay` | **P18:** internal-only bounded replay core. `RuntimeTraceReplayService` is the single owner; helpers `RuntimeTraceReplayEligibilityResolver`, `RuntimeTraceReplayInputLoader`, `RuntimeTraceReplayStrategy`. Reads traces only via `RuntimeTraceQueryService`; no `runtime_execution_trace` writes; no REST in P18. Domain types live under `domain.runtime.tracereplay`. |
| `application.service.runtime.tracereplaybatch` | **P27:** internal-only sequential batch over P18. `RuntimeTraceReplayBatchService` calls `RuntimeTraceReplayService.replay` with `byTraceId` only (plus `RuntimeTraceQueryService.listConversationTraceSummaries` for `BY_CONVERSATION` selection). **No** persistence, **no** `RuntimeTraceReplayComparisonBatchService` / `tracecomparisonbatch` / repository / `ProcessQueryService` / orchestrator. **P28** adds **`RuntimeTraceReplayBatchController` → `execute` only** (HTTP adapter; see `interfaces.rest.dto.tracereplaybatch`). **P29** adds replay-batch ZIP packaging in `tracereplaybatchexport` (export service → `execute` only). Domain types under `domain.runtime.tracereplaybatch`. |
| `application.service.runtime.tracecomparison` | **P19:** internal-only replay comparison core. `RuntimeTraceReplayComparisonService` is the single owner; `RuntimeTraceReplayComparator` performs bounded dimensional diff after `REPLAY_SUCCEEDED`. Reads originals via `RuntimeTraceQueryService` and replay via `RuntimeTraceReplayService` only; no persistence, no export inputs, **no REST endpoints in P19**. Domain types live under `domain.runtime.tracecomparison`. |
| `application.service.runtime.tracecomparisonbatch` | **P24:** internal-only sequential batch over P19. `RuntimeTraceReplayComparisonBatchService` calls `RuntimeTraceReplayComparisonService.compare` only (plus `RuntimeTraceQueryService.listConversationTraceSummaries` for `BY_CONVERSATION` selection). The service has **no** REST surface of its own, **no** persistence, **no** `RuntimeTraceReplayService` / export / repository / `ProcessQueryService` / orchestrator. **P25** adds **`RuntimeTraceReplayComparisonBatchController` → `execute` only** (HTTP adapter; see `interfaces.rest.dto.tracecomparisonbatch`). Domain types under `domain.runtime.tracecomparisonbatch`. |
| `application.service.runtime.traceregressionsuite` | **P30:** internal-only regression suite over P24. `RuntimeTraceRegressionSuiteService` calls **`RuntimeTraceReplayComparisonBatchService#execute` only** (once per suite entry, **≤20** entries, sequential). **No** REST, **no** persistence, **no** export, **no** `RuntimeTraceReplayComparisonService` / `RuntimeTraceReplayService` / `RuntimeTraceQueryService` / repository / `ProcessQueryService` / orchestrator. Domain types under `domain.runtime.traceregressionsuite`. **P31** adds **`RuntimeTraceRegressionSuiteController` → `execute` only** (HTTP adapter; see `interfaces.rest.dto.traceregressionsuite`; `RegressionSuiteRestJacksonConfiguration` strict JSON). **P46** adds **`RuntimeTraceRegressionSuiteRunController` → `execute` only** for two suite-run **`POST`** routes (after P31 **`HttpAdapter`** parse; then **`createRun`** on the run persistence service — orchestration **only** in the controller). **P47** adds **`RuntimeTraceRegressionSuiteDefinitionController` → `execute` only** for two saved-definition **`POST …/runs`** routes (after **`materializeToSuiteRequest`**; then **`createRun`** — orchestration **only** in the controller). |
| `application.service.runtime.traceregressionsuiteexport` | **P32:** regression-suite ZIP packaging. `RuntimeTraceRegressionSuiteExportService` calls **`RuntimeTraceRegressionSuiteService#execute` only** (once per request); `suite.json` from **`RuntimeTraceRegressionSuiteResponseDto.fromResult` only**; constructor-local `ObjectMapper`; **no** batch/replay/query/P17/P21/P23/P26/P29 export/orchestrator/repository. REST: **`RuntimeTraceRegressionSuiteExportController` → export service only** (never the suite service bean). |
| `application.service.runtime.traceregressionsuitedefinitionexecutionexport` | **P37:** saved-definition execution ZIP adapter. **`RuntimeTraceRegressionSuiteDefinitionExecutionExportService`** calls **`RuntimeTraceRegressionSuiteDefinitionService#materializeToSuiteRequest`** and **`RuntimeTraceRegressionSuiteService#execute` only** (plus P36-identical conversation guard on X2); **no** ownership of execute/materialize semantics; **no** P32 **`RuntimeTraceRegressionSuiteExportService`** dependency. REST: **`RuntimeTraceRegressionSuiteDefinitionExecutionExportController` → this service only**. |
| `application.service.runtime.traceregressionsuitedefinitionexport` | **P38:** ZIP export adapter (non-owning) for **saved** definition bytes only. **`RuntimeTraceRegressionSuiteDefinitionExportService`** calls **`RuntimeTraceRegressionSuiteDefinitionService#loadByIdForUser` only**; **`definition.json`** from **`RuntimeTraceRegressionSuiteDefinitionDetailDto.fromSnapshot`** + constructor-local **`ObjectMapper`**; **no** **`execute`**, **no** **`materializeToSuiteRequest`**, **no** `infrastructure.persistence` imports. REST: **`RuntimeTraceRegressionSuiteDefinitionExportController` → this service only**. |
| `application.service.runtime.traceregressionsuiterunexport` | **P43:** ZIP export adapter (non-owning) for **persisted** suite runs only. **`exportRunZip`** calls **`RuntimeTraceRegressionSuiteRunPersistenceService#loadByIdForUser` only**; **P53:** **`exportRunZipForDefinition`** calls **`#loadByIdForUserAndDefinition` only** (same **P43** ZIP layout / max size / **`ObjectMapper`**); **`run.json`** from **`RuntimeTraceRegressionSuiteRunDetailDto.fromSnapshot`** + constructor-local **FD4** **`ObjectMapper`**; **no** **`createRun`**, **`listSummariesForUser`**, **`execute`**, definition services, **no** `infrastructure.persistence` imports. REST: **`RuntimeTraceRegressionSuiteRunExportController` → `exportRunZip` only**; **`RuntimeTraceRegressionSuiteDefinitionController` → `exportRunZipForDefinition` only** (HTTP never calls the persistence **`@Service`** bean on those paths). |
| `application.service.runtime.traceregressionsuiterunimport` | **P44:** ZIP import adapter (non-owning). **`RuntimeTraceRegressionSuiteRunImportService`** validates a **P43-shaped** ZIP, deserializes **`run.json`** to **`RuntimeTraceRegressionSuiteRunDetailDto`** with constructor-local **FD4** **`ObjectMapper`**, builds **`RuntimeTraceRegressionSuiteResult`** via **`RuntimeTraceRegressionSuiteRunDetailDto#toRuntimeTraceRegressionSuiteResultForImport` only**, then calls **`RuntimeTraceRegressionSuiteRunPersistenceService#createRun` only**; **no** **`loadByIdForUser`**, **`listSummariesForUser`**, **`execute`**, definition services, **no** `infrastructure.persistence` imports. **P54:** **`importRunZipForDefinition`** accepts **P53-shaped** manifests (**`selectorType`** **`SAVED_DEFINITION_SCOPED_RUN`**) and **`createRun`** with **`SAVED_DEFINITION`** + path **`definitionId`** only — same sole import **`@Service`**. REST: **`RuntimeTraceRegressionSuiteRunImportController` → `importRunZip` only**; **`RuntimeTraceRegressionSuiteDefinitionController` → `importRunZipForDefinition` only** (`productBasePath` for **`Location`**). |
| `application.service.runtime.traceregressionsuiterunimportpreview` | **P45:** ZIP run import preview adapter (non-owning). **`RuntimeTraceRegressionSuiteRunImportPreviewService`** validates a **P43-shaped** ZIP (plus manifest semantic checks), deserializes **`run.json`** to **`RuntimeTraceRegressionSuiteRunDetailDto`** with constructor-local **FD4** **`ObjectMapper`**, returns **`RuntimeTraceRegressionSuiteRunImportPreviewResponseDto`** — **no** **`RuntimeTraceRegressionSuiteRunPersistenceService`**, **no** **`RuntimeTraceRegressionSuiteRunImportService`**, **no** persistence, **no** **`execute`**, definition services, **no** `infrastructure.persistence` imports. **P55:** **`previewImportZipForDefinition`** validates **P53-shaped** manifests in the **same** **`@Service`** (**no** second preview bean, **no** **`traceregressionsuiterunimport`** reuse). REST: **`RuntimeTraceRegressionSuiteRunImportPreviewController` → `previewImportZip` only**; **`RuntimeTraceRegressionSuiteDefinitionController` → `previewImportZipForDefinition` only** (no **`productBasePath`** bean on this service). |
| `application.service.runtime.traceregressionsuitedefinitionimport` | **P39:** ZIP import adapter (non-owning). **`RuntimeTraceRegressionSuiteDefinitionImportService`** validates a **P38-strict** ZIP, deserializes **`definition.json`**, builds **`CreateDefinitionCommand`** via **`RuntimeTraceRegressionSuiteDefinitionDetailDto#toCreateDefinitionCommand` only**, then calls **`RuntimeTraceRegressionSuiteDefinitionService#create` only**; **no** export beans, **no** `infrastructure.persistence` imports. REST: **`RuntimeTraceRegressionSuiteDefinitionImportController` → this service only** (`productBasePath` for **`Location`**). |
| `application.service.runtime.traceregressionsuitedefinitionimportpreview` | **P40:** ZIP import preview adapter (non-owning). **`RuntimeTraceRegressionSuiteDefinitionImportPreviewService`** applies the same P38-strict ZIP + manifest checks as **P39**, deserializes **`definition.json`** to **`RuntimeTraceRegressionSuiteDefinitionDetailDto`** with a constructor-local **FD4** **`ObjectMapper`**, returns **`RuntimeTraceRegressionSuiteDefinitionImportPreviewResponseDto`** — **no** **`RuntimeTraceRegressionSuiteDefinitionService`**, **no** **`RuntimeTraceRegressionSuiteDefinitionImportService`**, **no** persistence, **no** **`execute`**. REST: **`RuntimeTraceRegressionSuiteDefinitionImportPreviewController` → this service only**. |
| `application.service.runtime.traceregressionsuiterun` | **P41:** run snapshot persistence only. **`RuntimeTraceRegressionSuiteRunPersistenceService`** writes/reads **`runtime_trace_regression_suite_run`** and **`runtime_trace_regression_suite_run_entry`** from a supplied **`RuntimeTraceRegressionSuiteResult`** — **no** **`RuntimeTraceRegressionSuiteService`**, **no** **`RuntimeTraceRegressionSuiteDefinitionService`**, **no** REST in P41, **no** async. **P48:** **`deleteRunForUser`** is the **only** application delete entry point for persisted runs (**sole** storage-backed delete owner; **no** second **`@Service`**, **no** REST in P48). **P49:** **`DELETE …/{runId}`** on **`RuntimeTraceRegressionSuiteRunController`** calls **`deleteRunForUser`** **only** — **no** second persistence owner. **P50:** **`listSummariesForUserAndDefinition`** / **`loadByIdForUserAndDefinition`** on **`RuntimeTraceRegressionSuiteDefinitionController`** **`GET …/definitions/{definitionId}/runs`** (+ detail) — **no** global **`listSummariesForUser`**/**`loadByIdForUser`** on those routes, **no** second run-read **`@Service`**. **P51:** **`deleteRunForUserAndDefinition`** — **sole** definition-scoped delete on this **`@Service`** (**`deleteRunForUser`** unchanged). **P52:** **`DELETE …/{definitionId}/runs/{runId}`** on **`RuntimeTraceRegressionSuiteDefinitionController`** calls **`deleteRunForUserAndDefinition`** **only** (**no** two-arg **`deleteRunForUser`**). **P42:** the **`GET …/runtime-trace-regression-suite-runs`** HTTP adapter calls **`listSummariesForUser`** / **`loadByIdForUser`** on this service **only** (**no** second read owner, **no** **`createRun`** from the controller). **P46:** the two suite-run **`POST`** routes on **`RuntimeTraceRegressionSuiteRunController`** call **`createRun`** after **`RuntimeTraceRegressionSuiteService#execute`** (**`AD_HOC`**, empty **`definitionId`**) — **no** bridge orchestration **`@Service`**. **P47:** **`POST …/runtime-trace-regression-suite-definitions/{definitionId}/runs`** (and conversation-scoped **`POST …/runs`**) on **`RuntimeTraceRegressionSuiteDefinitionController`** call **`createRun`** with **`SAVED_DEFINITION`** and **`Optional.of(definitionId)`** after materialize + **`execute`** — **no** bridge orchestration **`@Service`**. **P43:** **`traceregressionsuiterunexport`** **`exportRunZip`** calls **`loadByIdForUser`** only for **`GET …/{runId}/export`** (**no** **`createRun`** / **`listSummariesForUser`** on that path). **P53:** **`exportRunZipForDefinition`** calls **`loadByIdForUserAndDefinition`** only for definition-scoped **`GET …/export`**. **P44:** **`traceregressionsuiterunimport`** calls **`createRun`** only for **`POST …/runtime-trace-regression-suite-runs/import`** (**no** **`loadByIdForUser`** / **`listSummariesForUser`** on that path). |
| `application.service.runtime.traceregressionsuitedefinition` | **P33:** internal-only persistence for **saved** regression suite definitions (`runtime_trace_regression_suite_*`). **`RuntimeTraceRegressionSuiteDefinitionService`** is the **sole** writer/reader; JPA only under `infrastructure.persistence.traceregressionsuitedefinition`; **materialize** builds **`RuntimeTraceRegressionSuiteRequest`** (P30) **without** calling **`RuntimeTraceRegressionSuiteService`**. **P34** adds **`listSummariesForUser`** / **`loadByIdForUser`** for **`GET`** on **`RuntimeTraceRegressionSuiteDefinitionController`**; **P35** mutations call **`create`** / **`update`** / **`delete`** only; **P36** HTTP execute calls **`materializeToSuiteRequest`** then delegates **`execute`** to **`RuntimeTraceRegressionSuiteService`** (no bridge service) — **no** second definition owner. **P47** run-creation **`POST`** routes on the same controller call **`materializeToSuiteRequest`** **only** (then **`execute`** + **`createRun`** on the run persistence service — orchestration **only** in the controller). **P50** adds definition-scoped run **`GET`** routes: **`loadByIdForUser`** **only** for gate (**no** **`materializeToSuiteRequest`** on those **`GET`**s). **P52** run **`DELETE`** uses **`loadByIdForUser`** **only** for gate (**no** **`materializeToSuiteRequest`**). **P53** run **`GET …/export`** uses **`loadByIdForUser`** **only** for gate (**ZIP** via **`traceregressionsuiterunexport`**). **P38** ZIP export is a separate package (**`traceregressionsuitedefinitionexport`**) that calls **`loadByIdForUser`** only. |
| `application.service.runtime.tracereplaybatchexport` | **P29:** replay-batch ZIP packaging. `RuntimeTraceReplayBatchExportService` calls **`RuntimeTraceReplayBatchService#execute` only** (once per request); `batch.json` from **`RuntimeTraceReplayBatchResponseDto.fromBatchResult` only**; constructor-local `ObjectMapper`; **no** comparison/query/P17/P21/P23/P26 export/orchestrator/repository. REST: **`RuntimeTraceReplayBatchExportController` → replay-batch export service only** (never the batch service bean). |
| `application.service.runtime.tracecomparisonbatchexport` | **P26:** batch replay-comparison ZIP packaging. `RuntimeTraceReplayComparisonBatchExportService` calls **`RuntimeTraceReplayComparisonBatchService#execute` only** (once per request); `batch.json` from **`RuntimeTraceReplayComparisonBatchResponseDto.fromBatchResult` only**; constructor-local `ObjectMapper`; **no** comparison/replay/query/P17/P21/P23 export/orchestrator/repository. REST: **`RuntimeTraceReplayComparisonBatchExportController` → batch export service only** (never the batch service bean). |
| `interfaces.rest.dto.tracecomparison` | **P20:** bounded HTTP projections (`RuntimeTraceReplayComparisonResponseDto`) for replay comparison GETs; static factories from `RuntimeTraceReplayComparisonResult`; **no** second comparison owner or mapper service. |
| `interfaces.rest.dto.tracecomparisonbatch` | **P25:** bounded batch POST request/response DTOs for replay comparison batches; strict JSON; static factories from P24 results; **no** dedicated mapper `@Service` bean. |
| `interfaces.rest.dto.traceregressionsuite` | **P31:** regression suite POST request/response DTOs; strict `ObjectMapper` bean; static `RuntimeTraceRegressionSuiteResponseDto.fromResult` only — **no** mapper `@Service`. **P36** reuses **`fromResult`** for saved-definition **`POST …/execute`** responses (no new response DTO family). **P46** reuses the same request DTO family for suite-run **`POST`** bodies only via **`RuntimeTraceRegressionSuiteHttpAdapter`** (**no** new request DTO types). |
| `interfaces.rest.dto.traceregressionsuitedefinition` | **P34:** read-only GET response DTOs for saved suite definitions; **P35:** strict JSON upsert request DTOs (`RuntimeTraceRegressionSuiteDefinitionUpsertRequestDto` and polymorphic entry types). **P38:** snapshot→**`RuntimeTraceRegressionSuiteDefinitionDetailDto`** mapping is **`RuntimeTraceRegressionSuiteDefinitionDetailDto.fromSnapshot`** only; **P39:** **`RuntimeTraceRegressionSuiteDefinitionDetailDto#toCreateDefinitionCommand`** for import (**`name`**, **`description`**, **`entries`** only). Upsert adapter mapping remains **`private static`** on **`RuntimeTraceRegressionSuiteDefinitionController`**. |
| `interfaces.rest.dto.traceregressionsuitedefinitionimportpreview` | **P40:** **`RuntimeTraceRegressionSuiteDefinitionImportPreviewResponseDto`** (preview JSON: **`definition`**, **`importable`**, **`warnings`**) for **`POST …/import/preview`**. |
| `interfaces.rest.dto.traceregressionsuiterunimportpreview` | **P45:** **`RuntimeTraceRegressionSuiteRunImportPreviewResponseDto`** (preview JSON: **`run`**, **`importable`**, **`warnings`**) for **`POST …/runtime-trace-regression-suite-runs/import/preview`**. |
| `interfaces.rest.dto.traceregressionsuiterun` | **P42:** persisted suite run list/detail JSON (**`RuntimeTraceRegressionSuiteRunListResponseDto`**, summary/detail/entry DTOs); **static factories only** from P41 read models; **no** mapper **`@Service`**, **no** `infrastructure.persistence` / JPA / Spring Data imports. |
| `interfaces.rest.dto.tracereplaybatch` | **P28:** bounded batch POST request/response DTOs for standalone replay batches; strict JSON (`ReplayBatchRestJacksonConfiguration`); static factories from P27 results; **no** dedicated mapper `@Service` bean. **P29** reuses the same DTOs for **`…/batch/export`** POST bodies only. |
| `interfaces.rest.dto.tracereplay` | **P22:** bounded HTTP projections (`RuntimeTraceReplayResponseDto`) for standalone replay GETs; static factory `fromReplayHttp` from `RuntimeTraceReplayResult` + path ids only; truncation/field selection only — **no** second replay owner. |
| `application.service.runtime.tracecomparisonexport` | **P21:** replay-comparison ZIP export (`RuntimeTraceReplayComparisonExportService`). Calls `RuntimeTraceReplayComparisonService` only; export-local bounded JSON for `manifest.json` / `comparison.json`; **no** `RuntimeTraceQueryService`, `RuntimeTraceReplayService`, P17 `RuntimeTraceExportService`, repository, or `RuntimeTraceReplayComparator`. REST: `RuntimeTraceReplayComparisonExportController` → export service only. |
| `application.service.runtime.tracereplayexport` | **P23:** standalone replay ZIP export (`RuntimeTraceReplayExportService`). Calls `RuntimeTraceReplayService.replay` only; `replay.json` from `RuntimeTraceReplayResponseDto.fromReplayHttp` only; **no** `RuntimeTraceQueryService`, P17 `RuntimeTraceExportService`, P19/P21 comparison export, repository, `ProcessQueryService`, or `RagExecutionOrchestrator`. REST: `RuntimeTraceReplayExportController` → export service only. |
| `application.service.runtime.tool` | Deterministic tools: `DeterministicToolStrategy` (sole entrypoint), resolver, executor, result mapper, `MeetingMinutesToolExecutionCore` (shared tool business execution) — invoked only from `RagExecutionOrchestrator` for P7 |
| `application.service.runtime.functioncalling` | P9 function calling: `FunctionCallingStrategy`, `FunctionCallingPolicyResolver`, `FunctionCallingToolRegistry`, `FunctionCallingExecutor`, `FunctionCallingResultMapper` — FC execution only from `RagExecutionOrchestrator` |
| `domain.runtime.tracereplaybatch` | **P27:** closed batch request/selection/result/summary/item types for standalone replay batches (`RuntimeTraceReplayBatchOutcome`, etc.) — transient aggregates only; capped item fields. |
| `domain.runtime.tracecomparisonbatch` | **P24:** closed batch request/selection/result/summary/item types (`RuntimeTraceReplayComparisonBatchOutcome`, etc.) — transient aggregates only; no mismatch lists on item rows. |
| `domain.runtime.traceregressionsuite` | **P30:** closed suite request/entry/entry-result/summary/outcome types — transient only; orchestrates multiple P24 batch invocations without redefining comparison semantics. |
| `domain.runtime.traceregressionsuiterun` | **P41:** persisted run read models (`RuntimeTraceRegressionSuiteRunSnapshot`, `RuntimeTraceRegressionSuiteRunSummary`, entry snapshots, ids/source types) — **no** Spring beans. |
| `domain.runtime.traceregressionsuitedefinition` | **P33:** commands, **`RuntimeTraceRegressionSuiteDefinitionSnapshot`**, sealed entry specs for persisted definitions — **no** JPA, **no** Spring beans. **P34:** **`RuntimeTraceRegressionSuiteDefinitionUserSummary`** for list rows. |
| `domain.runtime.engine` | `ExecutionContext`, `RagExecutionResult`, `ExecutionTrace`, snapshot selection records |
| `domain.runtime.advisor` | P10 advisor domain: `AdvisorKind`, `AdvisorDecision`, `AdvisorExecutionResult`, `PackedContextSet`, `PackedContextBlock`, `AdvisorOutcome`, suppression reasons |
| `domain.runtime.retrieval` | Advanced retrieval domain: `RetrievalRequest`, `RetrievalCandidate`, `CuratedContextSet`, `RetrievalDiagnostics`, `RetrievalMode`, fusion/rerank/compression outcomes |
| `domain.runtime.query` | Query understanding domain: `QueryPlan`, normalization/entities/rewrite results, intent/shape/ambiguity enums |
| `domain.runtime.clarification` | **P11:** `ClarificationOutcome`, `ClarificationQuestionKind`, `PendingClarificationState`, `ClarificationQuestion`, `ClarificationDecision`, `ClarificationExecutionResult` |
| `domain.runtime.memory` | **P12:** memory domain: mode/outcome/decision, history turn/slice, memory execution result and trace fragments |
| `domain.runtime.routing` | **P13:** routing domain: `AdaptiveRouteKind`, `AdaptiveRoutingOutcome`, `AdaptiveRoutingDecision`, `RouteExecutionGate`, `AdaptiveRoutingExecutionResult` |
| `domain.runtime.judge` | **P14:** judge domain: `JudgeCandidateSource`, `JudgeDecision`, `JudgeEvaluation`, `JudgeExecutionResult`, `JudgeOutcome` |
| `domain.runtime.tool` | Deterministic tool domain: `DeterministicToolKind`, `DeterministicToolDecision`, `DeterministicToolExecutionResult`, outcomes |
| `domain.runtime.functioncalling` | P9 FC domain: `FunctionCallingMode`, `FunctionCallingOutcome`, `FunctionCallingDecision`, `FunctionCallingExecutionResult` |
| `service.query.pipeline` | Preparation, synthesis, answer kernel (legacy; deterministic tools are not routed here) |
| `service.retriever` | Vector / corpus retrieval implementations |
| `service.config` | `ConfigResolver` (cascade merge owner), user/project configuration, sanitization; `JpaConfigurationSourceAdapter` implements `ConfigurationSourcePort` |
| `service.chat` | Chat-oriented orchestration helpers (if present) |
| `service.document` | Ingestion pipeline for project documents |
| `service.evaluation` | Minute evaluation and related services |
| `service.async` | Async tasks (e.g. lab jobs) |
| `service.preset` | RAG preset CRUD |
| `service.project` | Project-level operations |
| `service.model` | Allowed models / Ollama alignment |
| `observability` | Tracing helpers around evaluation |

**Webapp map:** App Router and API client live under `webapp/src/`; run `cd webapp && npm run doc` for TypeDoc.
