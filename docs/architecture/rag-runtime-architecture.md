# RAG runtime architecture

**Purpose:** Canonical **target** vocabulary for the **RAG Runtime Engine**. Each identifier below is **normative** for future code and documentation. Behaviour remains defined by code until aligned. See [ADR 0005](../adr/0005-target-rag-architecture-and-runtime-center.md).

**Related:** [target-architecture.md](target-architecture.md), [configuration-resolution-model.md](configuration-resolution-model.md), [knowledge-system-model.md](knowledge-system-model.md), [integration-flows.md](integration-flows.md).

## Orchestration and context

| Component | Responsibility | Conceptual inputs | Conceptual outputs / effects |
|-----------|----------------|-------------------|------------------------------|
| `ExecutionContext` | Aggregates per-request identity, workspace scope, **resolved runtime configuration**, active policies, and hooks for tracing. | Auth principal, project id, resolved config snapshot | Passed through pipelines and strategies |
| `PromptStack` | Ordered logical layers of messages/prompts feeding the LLM; aligns with `SystemPromptComposer` and ADR 0008. | Composed prompt layers, history slices | Normalized stack for model call |
| `RagExecutionOrchestrator` | Top-level coordinator for one RAG execution (product or lab). | `ExecutionContext`, user query | Final answer stream or structured result + `ExecutionTrace` |
| `ExecutionWorkflow` | Declared graph or sequence of steps for one execution kind (preset/route). | Selected workflow id, context | Ordered stages (understanding → retrieval → …) |
| `WorkflowSelector` | Chooses `ExecutionWorkflow` from **resolved config** using a deterministic matrix. | `ResolvedRuntimeConfig` | Workflow binding |
| `ExecutionTrace` | Structured record of decisions, stages, and timings for observability and lab reproducibility. | Events from orchestrator and pipelines | Trace export / logs / metrics |

## Pipelines

| Component | Responsibility | Typical placement in workflow |
|-----------|----------------|------------------------------|
| `QueryUnderstandingPipeline` | Builds a deterministic, structured `QueryPlan` (normalization → classification → entity extraction → structured rewrite → intent/shape/ambiguity). It is a mandatory runtime stage but has **no routing authority**. | Early stage |
| `RetrievalPipeline` | Selects strategies (vector, metadata, hybrid) and fetches context from the Knowledge System. **Implemented as** `AdvancedRetrievalPipeline` (single entrypoint for retrieval-capable workflows). | Mid stage |
| `PostRetrievalPipeline` | Ranks, compresses, or filters context; may invoke judges sufficiency before generation. | After retrieval, before / during generation |

## Tools and policy

| Component | Responsibility |
|-----------|----------------|
| `DeterministicToolStrategy` | Executes tools with fixed contracts (e.g. meeting-minutes metadata tools) without open-ended model tool loops. Sole deterministic tool entrypoint; invoked only from `RagExecutionOrchestrator` when P13 adaptive routing selects the deterministic-tool route family. |
| `MeetingMinutesToolExecutionCore` | Shared business execution for the five meeting-minutes `DeterministicToolKind` tools; used by deterministic execution and by P9 function calling after model tool selection (no duplicate semantics in FC). |
| `FunctionCallingStrategy` | P9: bounded Spring AI function-calling (single tool-enabled round, optional follow-up answer generation without tools). Sole FC entrypoint; invoked only from `RagExecutionOrchestrator` when config and `QueryPlan` gates pass. Final FC trace fields are written only by the orchestrator. |
| `FunctionCallingPolicyResolver` | Exposes the whitelisted tool subset from `QueryPlan` when FC is eligible; no LLM calls. |
| `ToolPolicy` | (Target) Allowed tools, rate limits, safety gates, and fallbacks per workflow / tenant / project. |

## Advisor, clarification, memory, routing

| Component | Responsibility |
|-----------|----------------|
| `AdvisorStrategy` | **P10 (implemented):** sole orchestrated advisor **execution** entrypoint; chains snapshot-bound retrieval (`RetrievalAdvisor` → `AdvancedRetrievalPipeline`) and deterministic packing (`ContextPackingAdvisor` → `PackedContextSet`). Invoked only from `RagExecutionOrchestrator`. |
| `AdvisorPolicyResolver` | **P10:** policy only (no LLM, no retrieval); produces `AdvisorDecision` for dense workflows when `useAdvisor` and gates pass. |
| `PackedContextSet` / `PackedContextBlock` | **P10:** immutable advisor-produced packed context; optional on `ExecutionContext` for dense workflows. |
| `AdvisorMode` | Operational mode for advisory behaviour (e.g. suggest vs act) as part of resolved config. |
| `ClarificationSession` | (Target) Tracks multi-turn clarification state when the runtime must resolve ambiguity before answering. **P11 (implemented):** pending state is stored only in `conversations.pending_clarification_jsonb`; orchestration lives under `application.service.runtime.clarification`. |
| `ClarificationQuestionGenerator` | **P11:** deterministic template map from `ClarificationQuestionKind` (no LLM). |
| `ClarifiedQueryRefiner` | **P11:** frozen merge `BASE:…` + newline + `QUESTION:…` + newline + `ANSWER:…` into `ExecutionContext.effectivePlanningInputText` for the next `QueryUnderstandingPipeline` call (no LLM). |
| `ConversationMemoryPolicy` | What history is injected, summarised, or dropped across turns. **P12 (implemented):** bounded runtime-owned memory stage under `application.service.runtime.memory` that loads persisted conversation history, selects a fixed slice, executes at most one LLM-backed condensation call, and produces the final `effectivePlanningInputText` for QU. |
| `AdaptiveRoutingStrategy` / `AdaptiveRoutingPolicyResolver` | **P13 (implemented):** deterministic, runtime-owned route-family selection stage that runs after clarification policy and before any execution-family stage. Produces a single `AdaptiveRoutingDecision` + `RouteExecutionGate` that the orchestrator consumes to enforce route-family exclusivity. |
| `AdaptiveRouteKind` / `AdaptiveRoutingOutcome` | **P13:** closed route-family set and terminal routing outcome set; summary is written only by `RagExecutionOrchestrator` into `ExecutionTrace`. |

## Judges

| Component | Responsibility |
|-----------|----------------|
| `JudgeStrategy` / `JudgePolicyResolver` | **P14 (implemented):** runtime-owned, orchestrator-only post-answer judge stage. Runs after the selected route family produces a candidate answer; can accept it or request exactly one bounded repair attempt (workflow candidates only in P14). Does not re-run clarification, memory, QU, or routing; does not change route family. |
| `JudgeCandidateSource` / `JudgeOutcome` | **P14:** candidate-source classification and closed terminal judge outcome set; summary fields are written only by `RagExecutionOrchestrator` into `ExecutionTrace`. |
| *(Target) Retrieval judges* | Future optional judges (e.g. retrieval sufficiency, faithfulness) belong as explicit runtime stages under orchestrator ownership, not hidden inside workflows or controllers. |

## Interactions (prose)

- `WorkflowSelector` is a lower-level workflow chooser used only when the selected route family permits workflow execution.
- `RagExecutionOrchestrator` drives pipelines and strategies in a single canonical runtime order, appending to `ExecutionTrace`. Order for orchestrated chat: `ExecutionContextFactory` (loads `pending_clarification_jsonb`, runs `ClarificationStateResolver` + `ClarifiedQueryRefiner`, sets `preMemoryPlanningInputText`) → **P12 memory stage** (loads persisted messages, selects a fixed slice, executes at most one LLM-backed condensation call, sets final `effectivePlanningInputText` and memory trace summaries) → **`QueryUnderstandingPipeline` (once per turn on final `effectivePlanningInputText`)** → **P11 clarification policy** (`ClarificationPolicyResolver`; on `ASKED_*` the orchestrator returns immediately with `workflowName=clarification` and does **not** run routing or any downstream execution families) → **P13 adaptive routing** (`AdaptiveRoutingStrategy`) → execute exactly one primary route family (workflow route, deterministic tools, function calling, or advisor) with at most one deterministic workflow fallback when the selected non-workflow family does not terminate the turn → **P14 judge** (post-answer, bounded evaluation + at most one repair attempt, never changes route family or re-runs upstream stages) → finalize response. `WorkflowSelector` is invoked only inside workflow-capable routes (direct/retrieval) and after successful advisor execution to choose a retrieval-capable workflow. Downstream non-selected families do not execute for that turn.
- **P15 trace persistence (implemented):** after the turn is finalized (including judge when enabled), the runtime boundary (`ProcessQueryService` / `SimpleProcessQueryService`) invokes `RuntimeTracePersistenceService` to append one best-effort `runtime_execution_trace` row containing a bounded JSON projection of the finalized `ExecutionTrace` plus linkage identifiers (`user_id`, `project_id`, optional `conversation_id`/`message_id`, `correlation_id`, optional `resolved_config_snapshot_id`/`config_hash`). Persistence never re-runs runtime stages and must not fail the response.
- **P16 trace query surfaces (implemented):** product REST exposes minimal, read-only endpoints to list per-conversation runtime trace summaries and fetch trace detail (by trace id or message id). All reads are owner-scoped and flow through a single application-layer owner `RuntimeTraceQueryService` which queries `runtime_execution_trace` directly (no second read model, no JSON scanning, no repository access from controllers).
- **P17 trace export core (implemented):** product REST exposes minimal, read-only endpoints to export either a single persisted runtime trace (by trace id or by conversation+message id) or a bounded per-conversation bundle. Exports are generated synchronously by `RuntimeTraceExportService` which depends only on `RuntimeTraceQueryService`, produces a deterministic ZIP (`manifest.json`, `traces/index.json`, per-trace JSON files), enforces a fixed count cap for bundles (200) and a hard ZIP size cap (10 MB, reject with 413), and excludes prompts/message content/ExecutionContext/replay instructions.
- **P18 runtime trace replay core (implemented, internal-only):** bounded re-execution of a **strict subset** of persisted orchestrated turns for debugging/regressions. Single application owner `RuntimeTraceReplayService` under `application.service.runtime.tracereplay`; **no REST in P18**. Trace rows are read **only** via `RuntimeTraceQueryService` (never `RuntimeExecutionTraceRepository` from replay code). Replay is **side-effect free** (no messages, no conversation/clarification/trace writes). It does **not** call `RagExecutionOrchestrator.execute`, `ProcessQueryService` / `SimpleProcessQueryService`, or `ExecutionContextFactory` as black-box builders; it pins the original route family and `workflowName` from the persisted trace, runs **one** `QueryUnderstandingPipeline` pass, reconstructs P12 memory from **historical** messages (`seq` strictly before the original user message), and executes only `DIRECT_WORKFLOW_ROUTE`, `RETRIEVAL_WORKFLOW_ROUTE` (pinned `usedKnowledgeSnapshotIds` from the trace JSON), or `DETERMINISTIC_TOOL_ROUTE` (pinned `deterministicToolKind`). Configuration is taken **only** from `resolved_config_snapshot_id` (materialized row); live conversation overrides are not used as semantic inputs. Unsupported traces return a closed `RuntimeTraceReplayOutcome`; execution failures after eligibility resolve to `REPLAY_FAILED_SAFE` (no route/workflow fallback).
- **P19 replay comparison core (implemented, internal-only):** deterministic, bounded comparison of **one** P16 trace detail row against **one** P18 replay result. Single application owner `RuntimeTraceReplayComparisonService` under `application.service.runtime.tracecomparison` with `RuntimeTraceReplayComparator`; **P19 does not add REST endpoints** (no controllers, no new HTTP surface). The service loads the original **only** via `RuntimeTraceQueryService`, invokes replay **only** via `RuntimeTraceReplayService`, branches on `RuntimeTraceReplayOutcome` before dimensional comparison, and returns a transient in-memory result (no persistence, no export inputs, no repository bypass). Closed outcomes and comparison dimensions are fixed in code; domain types live under `domain.runtime.tracecomparison`.
- **P20 runtime trace replay comparison query surfaces (implemented):** minimal **GET** product API on top of P19 **only**. `RuntimeTraceReplayComparisonController` under `interfaces.rest` delegates **directly** to `RuntimeTraceReplayComparisonService` (no second owner, no `RuntimeTraceQueryService` / `RuntimeTraceReplayService` / export / repository from the controller). **Exactly two** path-only routes under `rag.api.product-base-path`: by trace id and by conversation+message id; **any query string ⇒ 400**; **`ORIGINAL_TRACE_NOT_FOUND_OR_INACCESSIBLE` ⇒ 404** (not **403**); synchronous **200** with bounded JSON elsewhere. DTOs live under `interfaces.rest.dto.tracecomparison`; **no** persistence, **no** batch, **no** export coupling.
- **P21 replay comparison export core (implemented):** download **one** bounded replay-comparison result per request as a synchronous ZIP. Single application owner `RuntimeTraceReplayComparisonExportService` under `application.service.runtime.tracecomparisonexport` invokes **`RuntimeTraceReplayComparisonService.compare` only** (no `RuntimeTraceQueryService`, `RuntimeTraceReplayService`, P17 `RuntimeTraceExportService`, repository, or comparator). `RuntimeTraceReplayComparisonExportController` depends on **`RuntimeTraceReplayComparisonExportService` only** among application services. **Exactly two** GET routes under `rag.api.product-base-path` (`.../replay-comparison/export`) mirroring P20 selectors; ZIP contains **only** `manifest.json` and `comparison.json`; hard **2 MiB** ZIP cap (**413** on overflow); **any query string ⇒ 400**; **`ORIGINAL_TRACE_NOT_FOUND_OR_INACCESSIBLE` ⇒ 404**; **never** **403** or **202**; **no** persistence, **no** export history, **no** batch. Bounded field caps match P20 (512 / 50 / 256); `generatedAt` in the manifest means successive exports of the same trace are **not** byte-identical even when comparison inputs match.
- **P22 runtime trace replay query surfaces (implemented):** minimal **GET** product API for **standalone** P18 replay (not comparison/export). `RuntimeTraceReplayController` under `interfaces.rest` delegates **only** to `RuntimeTraceReplayService` (no `RuntimeTraceQueryService`, comparison/export services, repository, `ProcessQueryService`, or `RagExecutionOrchestrator` from the controller). **Exactly two** path-only routes under `rag.api.product-base-path`: by trace id (`.../runtime-traces/{traceId}/replay`) and by conversation+message id (`.../conversations/{conversationId}/messages/{messageId}/runtime-trace/replay`); **any query string ⇒ 400**; **`NotFoundException`** from persisted trace resolution (P16 read path inside replay) **⇒ 404**; synchronous **200** with bounded JSON for **every** returned `RuntimeTraceReplayResult`, including all **`UNSUPPORTED_*`** and **`REPLAY_FAILED_SAFE`** (**never** map those to **404**); **never** **403** or **202**. JSON is built only via `RuntimeTraceReplayResponseDto.fromReplayHttp` (`interfaces.rest.dto.tracereplay`); **no** `executionTraceJson` / full stages / prompts / `ExecutionContext` bodies.
- **P23 runtime trace replay export core (implemented):** download **one** bounded standalone replay result per request as a synchronous **ZIP** parallel to P22 (adds **`.../replay/export`** only). Single packaging owner `RuntimeTraceReplayExportService` under `application.service.runtime.tracereplayexport` calls **`RuntimeTraceReplayService.replay` exactly once** per request (no `RuntimeTraceQueryService`, P17 `RuntimeTraceExportService`, P19/P21 comparison services, repository, `ProcessQueryService`, or `RagExecutionOrchestrator`). `RuntimeTraceReplayExportController` depends on **`RuntimeTraceReplayExportService` only** among application beans. ZIP root contains **only** `manifest.json` then `replay.json`; `replay.json` is **only** `RuntimeTraceReplayResponseDto.fromReplayHttp` serialized with a constructor-local `ObjectMapper`; `manifest.json` includes `exportKind: REPLAY`, `schemaVersion: 1`, `truncated: false`, and `zipSizeBytes` equal to the **final** response body length (fixed-point assembly). Hard **2 MiB** cap on the final archive (**413** when exceeded, **no** response bytes); **any query string ⇒ 400**; **`NotFoundException`** on unresolved/inaccessible persisted trace scope **⇒ 404**; **200** + ZIP when replay returns **any** `RuntimeTraceReplayResult` after trace resolution and size cap passes — including **`UNSUPPORTED_*`** and **`REPLAY_FAILED_SAFE`**; **never** **403** or **202**.
- **P24 replay comparison batch core (implemented, internal-only):** synchronous, **sequential** orchestration over **up to 50** P19 **`compare`** calls per invocation. Single owner `RuntimeTraceReplayComparisonBatchService` under `application.service.runtime.tracecomparisonbatch` depends only on **`RuntimeTraceReplayComparisonService`** and **`RuntimeTraceQueryService`** (for **`listConversationTraceSummaries`** in **`BY_CONVERSATION`** mode). Selection modes: **`BY_TRACE_IDS`** (ordered list, **≤50** raw ids before dedup, first-seen dedup) and **`BY_CONVERSATION`** (first **50** rows, **`page=0`**, **`createdAt DESC`** via P16 listing — **no** pagination API). Each item uses **`RuntimeTraceReplayComparisonRequest.byTraceId`** only. **No** REST, **no** persistence, **no** export, **no** `RuntimeTraceReplayService`, repository, `ProcessQueryService`, orchestrator, or **async**. Transient results live under **`domain.runtime.tracecomparisonbatch`** (batch outcome, per-item counters + bounded summary **512**, **no** mismatch lists on items).
- **P30 runtime trace regression suite core (implemented, internal-only):** single owner `RuntimeTraceRegressionSuiteService` under `application.service.runtime.traceregressionsuite` orchestrates an **ordered** list of **≤20** suite entries by calling **`RuntimeTraceReplayComparisonBatchService#execute` exactly once per entry**, **sequentially** — the **only** allowed batch delegate (owner chain: suite → P24 **`execute`**). Each entry maps to P24 **`BY_TRACE_IDS`** or **`BY_CONVERSATION`** via the same **`RuntimeTraceReplayComparisonBatchRequest`** factories as P24/P25. **No** REST, **no** persistence, **no** export (P26 remains the batch export surface), **no** async, **no** caching of suite results. Closed transient types under **`domain.runtime.traceregressionsuite`** (suite request/entry/results/summary/outcome); P30 copies P24 batch scalars per entry **without** retaining **`RuntimeTraceReplayComparisonBatchResult`** references on the suite result.
- **P31 runtime trace regression suite query surfaces (implemented):** **HTTP adapter only** — **two** **`POST`** routes under `rag.api.product-base-path`: **`.../runtime-traces/regression-suite`** (explicit discriminated suite entries) and **`.../conversations/{conversationId}/runtime-traces/regression-suite`** (path **`conversationId`** + batch filter specs only). `RuntimeTraceRegressionSuiteController` depends on **`RuntimeTraceRegressionSuiteService` only** among application services and calls **`execute`** only. Strict JSON via dedicated **`ObjectMapper`** (`FAIL_ON_UNKNOWN_PROPERTIES` — unknown keys **400**); **query string on either route ⇒ 400**; **`entries` length ≤20** validated **before** **`execute`**; nested polymorphic rules **400** before service (not **`NOT_ATTEMPTED`** as primary validation). **`RuntimeTraceRegressionSuiteOutcome.NOT_ATTEMPTED` ⇒ 400** empty body (defensive); **`EMPTY_SUITE` ⇒ 200** with **`entries: []`**; **no** **404** on these **`POST`**s (absence/failures are **200** entry rows per P30). Bounded DTOs under **`interfaces.rest.dto.traceregressionsuite`** (`fromResult` static only); **no** export, persistence, or async.
- **P32 runtime trace regression suite export core (implemented):** **two** **`POST`** ZIP routes only (`.../runtime-traces/regression-suite/export` and `.../conversations/{conversationId}/runtime-traces/regression-suite/export`). `RuntimeTraceRegressionSuiteExportController` depends on **`RuntimeTraceRegressionSuiteExportService` only** (never **`RuntimeTraceRegressionSuiteService`** directly). The export service calls **`RuntimeTraceRegressionSuiteService#execute` exactly once** per request; **`suite.json`** is **only** **`RuntimeTraceRegressionSuiteResponseDto.fromResult`** serialized with a constructor-local **`ObjectMapper`**; ZIP root is **`manifest.json`** (`exportKind: REGRESSION_SUITE`, `schemaVersion` JSON integer **1**, `truncated: false`, **`zipSizeBytes`** fixed-point equals final body length) then **`suite.json`**. P31 request DTOs and strict validation reused; **`NOT_ATTEMPTED` ⇒ 400** (no ZIP); final ZIP **≤ 2 MiB** else **413**; **no** route-level **404** / **403** / **202** on P32 **`POST`**s.
- **P33 runtime trace regression suite definition persistence core (implemented, internal-only):** durable **named suite definitions** for a user (`runtime_trace_regression_suite_*` tables — see [DATA_MODEL.md](DATA_MODEL.md) §6.4). Single owner **`RuntimeTraceRegressionSuiteDefinitionService`** under **`application.service.runtime.traceregressionsuitedefinition`** performs create / full-replace update / delete / load / **materialize** to **`RuntimeTraceRegressionSuiteRequest`** (P30 request shape) **without** invoking **`RuntimeTraceRegressionSuiteService`** or any export/replay/comparison/query/orchestration beans. **No** REST in P33; execution remains **P30** only.
- **P34 runtime trace regression suite definition query surfaces (implemented):** **two** owner-scoped **`GET`** routes on **`RuntimeTraceRegressionSuiteDefinitionController`** under **`rag.api.product-base-path`**: **`…/runtime-trace-regression-suite-definitions`** (list) and **`…/runtime-trace-regression-suite-definitions/{definitionId}`** (detail). **`GET`** handlers call **`RuntimeTraceRegressionSuiteDefinitionService`** for **`listSummariesForUser`** / **`loadByIdForUser`** only — **no** query string (**`400`** empty body if present), list body **`{"definitions":[…]}`** (never a top-level array), detail **`404`** with **`NotFoundException("definition not found")`** when the definition is missing or not owned; **no** suite **`execute`**, **no** export, **no** mutating definition methods on these **`GET`** routes. DTOs under **`interfaces.rest.dto.traceregressionsuitedefinition`**; snapshot→detail mapping is **`RuntimeTraceRegressionSuiteDefinitionDetailDto.fromSnapshot`** only (**P38**).
- **P35 runtime trace regression suite definition mutation surfaces (implemented):** the **same** **`RuntimeTraceRegressionSuiteDefinitionController`** adds **`POST …/runtime-trace-regression-suite-definitions`**, **`PUT …/runtime-trace-regression-suite-definitions/{definitionId}`**, and **`DELETE …/runtime-trace-regression-suite-definitions/{definitionId}`**. Mutations call **`RuntimeTraceRegressionSuiteDefinitionService#create`**, **`#update`**, **`#delete`** only — **never** **`listSummariesForUser`**, **`loadByIdForUser`**, **`materializeToSuiteRequest`**, **`RuntimeTraceRegressionSuiteService`**, or any export/replay/comparison/query/orchestration beans. **Any query string on any handler of this controller ⇒ `400`** empty body (see P36 for seven routes total). Strict JSON for **`POST`/`PUT`** uses the dedicated bean **`definitionMutationStrictObjectMapper`** (`com.uniovi.rag.configuration.RegressionSuiteDefinitionMutationJacksonConfiguration`: **`FAIL_ON_UNKNOWN_PROPERTIES`**, **`JavaTimeModule`**, not the application default **`ObjectMapper`**); **`POST`/`PUT`** share one root DTO **`RuntimeTraceRegressionSuiteDefinitionUpsertRequestDto`**; command mapping is **`private static`** on the controller only. **`Location`** on create: **`{rag.api.product-base-path}/runtime-trace-regression-suite-definitions/{id}`** (no trailing slash on the base path property). Success: **`201`** empty body + **`Location`**, **`204`** empty body for successful **`PUT`**/**`DELETE`**; duplicate name **`IllegalStateException` ⇒ `409`** empty body; **`NotFoundException`** from **`update`**/**`delete` ⇒ `404`** empty body (same shape for missing row and wrong owner); **`IllegalArgumentException`** where caught ⇒ **`400`** empty body. **No** suite execution on these mutation routes, **no** ZIP, **no** schema change.
- **P36 runtime trace regression suite definition execution surfaces (implemented):** **two** **`POST`** routes on the **same** **`RuntimeTraceRegressionSuiteDefinitionController`**: **`POST …/runtime-trace-regression-suite-definitions/{definitionId}/execute`** (E1) and **`POST …/conversations/{conversationId}/runtime-trace-regression-suite-definitions/{definitionId}/execute`** (E2). **No** bridge **`@Service`**: the controller composes **`definitionService.materializeToSuiteRequest(definitionId, userId)`** then **`suiteService.execute(req)`** only (O1 **`RuntimeTraceRegressionSuiteService`**, O2 **`RuntimeTraceRegressionSuiteDefinitionService`**). **`NotFoundException`** from **`materializeToSuiteRequest`** is caught **only** around that call (**`404`** empty); **`execute`** is **outside** that **`catch`** (AC1). E2: after materialize, a **read-only** guard on **`req.entries()`** (**FD19**) returns **`404`** if there is no **`ByConversation`** entry or any **`ByConversation.conversationId`** differs from the path — **the same `req` instance** is passed to **`execute`** (**FD24**; no filtering or rebuild). Request body: **`null`** or blank after **`trim()`** only; any other bytes ⇒ **`400`** before materialize (**AC2**). Success body is **only** **`RuntimeTraceRegressionSuiteResponseDto.fromResult(result)`** on **`200`** (same rule as P31: **`NOT_ATTEMPTED` ⇒ `400`** empty; **`EMPTY_SUITE`** / **`COMPLETED_*` ⇒ `200`** JSON). **Never** **`403`** or **`202`** on E1/E2 (**FD13**). E1/E2 **never** call **`create`**, **`update`**, **`delete`**, **`listSummariesForUser`**, or **`loadByIdForUser`** on the definition service (**O5**). **No** ZIP export on this controller, **no** async/executors, **no** persistence of suite run results, **no** schema change.
- **P47 runtime trace regression suite run creation from definition surfaces (implemented):** **two** **`POST`** routes on the **same** **`RuntimeTraceRegressionSuiteDefinitionController`**: **`POST …/runtime-trace-regression-suite-definitions/{definitionId}/runs`** and **`POST …/conversations/{conversationId}/runtime-trace-regression-suite-definitions/{definitionId}/runs`**. **No** **`consumes`** attribute (**P36** parity). **No** bridge **`@Service`**: **`materializeToSuiteRequest`** → **`suiteService.execute(req)`** (same **`req`** reference as **P36** **FD-req-ref**) → **`RuntimeTraceRegressionSuiteRunPersistenceService#createRun`** with **`SAVED_DEFINITION`** and **`Optional.of(definitionId)`** when **`suiteOutcome` ∈ {`EMPTY_SUITE`, `COMPLETED_ALL_BATCH_RETURNS`, `COMPLETED_WITH_ENTRY_EXECUTION_FAILURES`}** — **`NOT_ATTEMPTED` ⇒ `400`** empty (**no** **`createRun`**). **FD7** first; **FD-parse** path UUIDs; body rule **`isExecuteBodyAllowed`** (same as **P36** execute); **`NotFoundException`** from materialize ⇒ **`404`** empty (**no** **`execute`**, **no** **`createRun`**); route 2: **unchanged** **`conversationScopedGuardFails`** after materialize and **before** **`execute`** (**FD19-freeze**). Success **`201`** empty + **`Location`** **`{rag.api.product-base-path}/runtime-trace-regression-suite-runs/{newRunId}`** (**no** **`Location`** on **400**/**404** from these handlers). **No** **`RuntimeTraceRegressionSuiteResponseDto`** on these routes. **`mvn test`** in **`rag-service`**.
- **P37 runtime trace regression suite definition execution export surfaces (implemented):** **two** **`POST`** ZIP routes on **`RuntimeTraceRegressionSuiteDefinitionExecutionExportController`** only: **`POST …/runtime-trace-regression-suite-definitions/{definitionId}/execute/export`** (X1) and **`POST …/conversations/{conversationId}/runtime-trace-regression-suite-definitions/{definitionId}/execute/export`** (X2). The controller injects **`RuntimeTraceRegressionSuiteDefinitionExecutionExportService` only**; that service calls **`materializeToSuiteRequest`** then (**X2** only) the **same P36 FD19** conversation guard on the **same `req`**, then **`execute` once**, then builds a **P32-shaped** two-entry **STORED** ZIP (**`manifest.json`** then **`suite.json`**; **`exportKind: REGRESSION_SUITE_DEFINITION_EXECUTION`**; **`zipSizeBytes`** converges to final length; **`truncated: false`**; **`generatedAt`** fixed for the convergence loop). **`suite.json`** bytes are **only** **`objectMapper.writeValueAsBytes(RuntimeTraceRegressionSuiteResponseDto.fromResult(result))`** using a constructor-local **`ObjectMapper`** (same feature set as P32 suite export). **`NOT_ATTEMPTED`** after **`execute`** ⇒ **`400`** empty (no ZIP); **`zipBytes.length > 2097152`** ⇒ **`413`** empty after **`execute`** completed once. **400**/**404**/**413** responses obey empty body and **no** **`application/zip`** / **`attachment`** disposition (**FD31**). **No** bridge second owner, **no** **`RuntimeTraceRegressionSuiteExportService`** reuse for this flow, **no** schema change, **no** persisted export history, **no** async.
- **P38 runtime trace regression suite definition export surfaces (implemented):** **one** synchronous **`GET …/runtime-trace-regression-suite-definitions/{definitionId}/export`** on **`RuntimeTraceRegressionSuiteDefinitionExportController` → `RuntimeTraceRegressionSuiteDefinitionExportService` → `RuntimeTraceRegressionSuiteDefinitionService#loadByIdForUser` only** (no **`execute`**, no **`materializeToSuiteRequest`**, no repositories in the export adapter). **`definition.json`** is **only** **`objectMapper.writeValueAsBytes(RuntimeTraceRegressionSuiteDefinitionDetailDto.fromSnapshot(snapshot))`** with the export service’s constructor-local **`ObjectMapper`** (same feature flags as P32/P37 export mappers). Two-entry **STORED** ZIP: **`manifest.json`** (**`exportKind: REGRESSION_SUITE_DEFINITION`**, **`selectorType: SAVED_DEFINITION_BY_ID`**, **`zipSizeBytes`** converged) then **`definition.json`**. Query string or bad UUID ⇒ **`400`** empty; missing/wrong owner ⇒ **`404`** empty; ZIP **> 2097152** bytes ⇒ **`413`** empty. **No** persisted export history, **no** async. Regression coverage: **`mvn test`** in **`rag-service`** (see module README).
- **P39 runtime trace regression suite definition import surfaces (implemented):** **one** synchronous **`POST …/runtime-trace-regression-suite-definitions/import`** on **`RuntimeTraceRegressionSuiteDefinitionImportController` → `RuntimeTraceRegressionSuiteDefinitionImportService` → `RuntimeTraceRegressionSuiteDefinitionService#create` only** (strict **P38-shaped** **STORED** ZIP: **`manifest.json`** then **`definition.json`**, **`exportKind`**, **`schemaVersion` 1**, **`truncated` false**, **`zipSizeBytes == body.length`**). Raw body **`application/zip`** only (**no** parameters on **`Content-Type`**), **≤ 2097152** bytes; query string ⇒ **`400`**. **`definition.json`** → **`RuntimeTraceRegressionSuiteDefinitionDetailDto`** via import service **FD4** **`ObjectMapper`**, then **`CreateDefinitionCommand`** **only** via **`detail.toCreateDefinitionCommand()`** (ignores persisted metadata fields on the DTO). **`IllegalStateException`** from **`create`** (duplicate name) ⇒ **`409`** empty body — **no** rename/suffix/overwrite (**FD-no-rename**). **`RuntimeTraceRegressionSuiteDefinitionImportRejectedException`** / **`IllegalArgumentException`** from validation ⇒ **`400`** empty. Success **`201`** + **`Location`** same concatenation rule as P35 **`POST`** create. **No** **`execute`**, **`materializeToSuiteRequest`**, **`loadByIdForUser`**, **`update`**, **`delete`**, export beans, multipart, or async. **`mvn test`** in **`rag-service`**.
- **P40 runtime trace regression suite definition import preview surfaces (implemented):** **one** synchronous **`POST …/runtime-trace-regression-suite-definitions/import/preview`** on **`RuntimeTraceRegressionSuiteDefinitionImportPreviewController` → `RuntimeTraceRegressionSuiteDefinitionImportPreviewService` only**. Same **P38-strict** **STORED** ZIP and manifest rules as **P39**; raw **`application/zip`** only (**no** **`Content-Type`** parameters), **≤ 2097152** bytes; failures ⇒ **`400`** empty (**`RuntimeTraceRegressionSuiteDefinitionImportPreviewRejectedException`**). Success **`200`** **`application/json`**: **`RuntimeTraceRegressionSuiteDefinitionImportPreviewResponseDto`** with **`definition`** from **`ObjectMapper#readValue`** on **`definition.json`**, **`importable` true**, empty **`warnings`** — **no** **`RuntimeTraceRegressionSuiteDefinitionService`**, **no** **`RuntimeTraceRegressionSuiteDefinitionImportService`**, **no** **`toCreateDefinitionCommand`**, **no** persistence or **`execute`**. **`mvn test`** in **`rag-service`**.
- **P41 runtime trace regression suite run persistence core (implemented):** **internal-only** durable storage for **`RuntimeTraceRegressionSuiteResult`** snapshots: Flyway **`V33`** tables **`runtime_trace_regression_suite_run`** + **`runtime_trace_regression_suite_run_entry`** (scalar columns and **CHECK**s only — **no** JSONB, arrays, or trace blobs). **`RuntimeTraceRegressionSuiteRunPersistenceService`** is the **sole** Spring owner for **P41** run table I/O (**`createRun`**, **`loadByIdForUser`**, **`listSummariesForUser`**); **`ORDER BY created_at DESC, id ASC`** for listing. **No** **`RuntimeTraceRegressionSuiteService#execute`**, **no** **`RuntimeTraceRegressionSuiteDefinitionService`**, **no** REST, **no** async. **`definition_id`** on runs is an opaque UUID when **`source_type = SAVED_DEFINITION`** (**no FK** to definition rows). See **`DATA_MODEL.md`** §6.5.
- **P48 runtime trace regression suite run deletion core (implemented, internal-only):** owner-scoped delete via **`RuntimeTraceRegressionSuiteRunPersistenceService#deleteRunForUser`** only — **`RuntimeTraceRegressionSuiteRunRepository#deleteByIdAndUserId`** on **`runtime_trace_regression_suite_run`**; **`runtime_trace_regression_suite_run_entry`** rows are removed **only** by **`V33`** **`ON DELETE CASCADE`** (**no** entry repository calls, **no** manual entry deletes). **`boolean`** return (**`true`** iff one parent row deleted); missing or wrong-owner id ⇒ **`false`** (**no** **`NotFoundException`**). **No** REST, **no** Flyway/DDL in **P48**; a future HTTP delete would call **`deleteRunForUser`** without a second persistence owner (**OW-P41** sole create/read/delete).
- **P42 runtime trace regression suite run query surfaces (implemented):** **two** **`GET`** routes on **`RuntimeTraceRegressionSuiteRunController` → `RuntimeTraceRegressionSuiteRunPersistenceService` only**: **`…/runtime-trace-regression-suite-runs`** (list) and **`…/runtime-trace-regression-suite-runs/{runId}`** (detail). Any query string ⇒ **`400`** empty body; malformed **`runId`** ⇒ **`400`**; missing or not owned run ⇒ **`404`** with **`NotFoundException("run not found")`**; list **`200`** body is **`{"runs":[…]}`** with that sole top-level key; detail **`200`** uses **`RuntimeTraceRegressionSuiteRunDetailDto.fromSnapshot`** (flat summary scalars + **`entries`**, sorted by **`entryOrder`**, per-entry **`selectorEcho`** / **`failureDetail`** truncation at the HTTP DTO layer only). **No** **`createRun`** from the controller, **no** suite **`execute`**, **no** definition or export/replay/query/orchestrator beans on this adapter; DTO mapping is **static factories** under **`interfaces.rest.dto.traceregressionsuiterun`** only. **`mvn test`** in **`rag-service`**.
- **P46 runtime trace regression suite run creation surfaces (implemented):** **two** **`POST`** routes on the **same** **`RuntimeTraceRegressionSuiteRunController`**: **`POST …/runtime-trace-regression-suite-runs`** (P31 explicit **`RuntimeTraceRegressionSuiteExecuteRequestDto`**) and **`POST …/conversations/{conversationId}/runtime-trace-regression-suite-runs`** (conversation **`RuntimeTraceRegressionSuiteConversationExecuteRequestDto`**). **No** bridge **`@Service`**: **`RuntimeTraceRegressionSuiteHttpAdapter.parseExplicit` / `parseConversationScoped`** with **`RegressionSuiteRestJacksonConfiguration`** strict **`ObjectMapper`** only (**`@RequestBody String`**, **`FAIL_ON_UNKNOWN_PROPERTIES`**), then **`RuntimeTraceRegressionSuiteService#execute` once**, then **`RuntimeTraceRegressionSuiteRunPersistenceService#createRun` once** with **`AD_HOC`** and **`Optional.empty()`** for **`definitionId`** when **`suiteOutcome` ∈ {`EMPTY_SUITE`, `COMPLETED_ALL_BATCH_RETURNS`, `COMPLETED_WITH_ENTRY_EXECUTION_FAILURES`}** — **`NOT_ATTEMPTED` ⇒ `400`** empty (**no** **`createRun`**). Success **`201`** empty + **`Location`** **`{rag.api.product-base-path}/runtime-trace-regression-suite-runs/{newRunId}`** (no query on **`Location`**). Any query string on either **`POST` ⇒ `400`** empty (**no** **`execute`**, **no** **`createRun`**). Parse failures ⇒ **`400`** empty. **`GET`** list/detail unchanged (**P42**). **`mvn test`** in **`rag-service`**.
- **P43 runtime trace regression suite run export surfaces (implemented):** **one** synchronous **`GET …/runtime-trace-regression-suite-runs/{runId}/export`** on **`RuntimeTraceRegressionSuiteRunExportController` → `RuntimeTraceRegressionSuiteRunExportService` → `RuntimeTraceRegressionSuiteRunPersistenceService#loadByIdForUser` only** (ZIP adapter — **no** storage ownership). Path-only (**any query string ⇒ `400`** empty); bad UUID ⇒ **`400`**; missing / wrong owner ⇒ **`404`** empty; assembled ZIP **> 2097152** bytes ⇒ **`413`** empty. **200** returns **STORED** ZIP: **`manifest.json`** (**`exportKind: REGRESSION_SUITE_RUN`**, **`selectorType: SAVED_RUN_BY_ID`**, converged **`zipSizeBytes`**) then **`run.json`** from **`RuntimeTraceRegressionSuiteRunDetailDto.fromSnapshot`** + constructor-local **FD4** **`ObjectMapper`** only — **no** suite **`execute`**, **no** definition services, **no** second bridge **`@Service`**. **`mvn test`** in **`rag-service`**.
- **P44 runtime trace regression suite run import surfaces (implemented):** **one** synchronous **`POST …/runtime-trace-regression-suite-runs/import`** on **`RuntimeTraceRegressionSuiteRunImportController` → `RuntimeTraceRegressionSuiteRunImportService` → `RuntimeTraceRegressionSuiteRunPersistenceService#createRun` only** (strict **P43-shaped** **STORED** ZIP: **`manifest.json`** then **`run.json`**, **`exportKind`**, **`schemaVersion` 1**, **`truncated` false**, **`zipSizeBytes == body.length`**, manifest/run coherence). Raw body **`application/zip`** only (**no** parameters on **`Content-Type`**), **1–2097152** bytes; query string ⇒ **`400`**. **`run.json`** → **`RuntimeTraceRegressionSuiteRunDetailDto`** via import service **FD4** **`ObjectMapper#readValue`**, then **`RuntimeTraceRegressionSuiteResult`** **only** via **`detail.toRuntimeTraceRegressionSuiteResultForImport()`** — **no** artifact **`id`**/**`createdAt`** as persisted PK/timestamp (**P41** assigns new ids). **`RuntimeTraceRegressionSuiteRunImportRejectedException`** / **`IllegalArgumentException`** ⇒ **`400`** empty; success **`201`** empty + **`Location`** **`{productBasePath}/runtime-trace-regression-suite-runs/{newRunId}`**. **No** suite **`execute`**, **no** definition services, **no** **`loadByIdForUser`**/**`listSummariesForUser`** on this path, **no** second bridge **`@Service`**. **`mvn test`** in **`rag-service`**.
- **P45 runtime trace regression suite run import preview surfaces (implemented):** **one** synchronous **`POST …/runtime-trace-regression-suite-runs/import/preview`** on **`RuntimeTraceRegressionSuiteRunImportPreviewController` → `RuntimeTraceRegressionSuiteRunImportPreviewService` only** (strict **P43-shaped** **STORED** ZIP + manifest semantic checks + manifest/run coherence + source pairing; **no** persistence, **no** **`RuntimeTraceRegressionSuiteRunPersistenceService`**, **no** **`RuntimeTraceRegressionSuiteRunImportService`**). Raw **`application/zip`** only (**no** **`Content-Type`** parameters), **1–2097152** bytes; query string ⇒ **`400`**. **`run.json`** → **`RuntimeTraceRegressionSuiteRunDetailDto`** via preview service **FD4** **`ObjectMapper#readValue`** only; success **`200`** **`application/json`**: **`RuntimeTraceRegressionSuiteRunImportPreviewResponseDto`** with **`run`** the same **`DetailDto`** reference, **`importable` true**, empty **`warnings`** — **no** **`Location`**, **no** **`createRun`**. **`RuntimeTraceRegressionSuiteRunImportPreviewRejectedException`** / **`IllegalArgumentException`** ⇒ **`400`** empty. **`mvn test`** in **`rag-service`**.
- **P25 runtime trace replay comparison batch query surfaces (implemented):** **HTTP adapter only** — **two** **`POST`** routes under `rag.api.product-base-path` (`.../runtime-traces/replay-comparisons/batch` for **`BY_TRACE_IDS`** and `.../conversations/{conversationId}/runtime-traces/replay-comparisons/batch` for **`BY_CONVERSATION`**). `RuntimeTraceReplayComparisonBatchController` depends on **`RuntimeTraceReplayComparisonBatchService` only** among application services and calls **`execute`** only. Strict JSON bodies (unknown keys / wrong types ⇒ **400**); **`NOT_ATTEMPTED`** ⇒ **400** (never **200**); trace-id route **never** uses route-level **404** for per-trace access problems (they appear as item rows); conversation route may **404** only when conversation scope is missing/inaccessible (**`NotFoundException`**); empty conversation selection ⇒ **200** **`EMPTY_SELECTION`**. Bounded DTOs under **`interfaces.rest.dto.tracecomparisonbatch`** (items **≤50**, summary **≤512**, counters-only — **no** mismatch blobs). Semantics and selection rules remain **P24** — P25 does not change batch logic.
- **P26 runtime trace replay comparison batch export core (implemented):** **two** **`POST`** ZIP download routes only (`.../runtime-traces/replay-comparisons/batch/export` and `.../conversations/{conversationId}/runtime-traces/replay-comparisons/batch/export`). `RuntimeTraceReplayComparisonBatchExportController` depends on **`RuntimeTraceReplayComparisonBatchExportService` only** (never **`RuntimeTraceReplayComparisonBatchService`** directly). The export service calls **`RuntimeTraceReplayComparisonBatchService#execute` exactly once** per request and packages **`manifest.json`** (export-specific schema, `exportKind: REPLAY_COMPARISON_BATCH`, `schemaVersion` JSON integer **1**, `truncated: false`, `zipSizeBytes` fixed-point match) then **`batch.json`** — **only** **`RuntimeTraceReplayComparisonBatchResponseDto.fromBatchResult`** serialized (same shape as P25 **200**). P25 request DTOs reused; strict-schema **400** rules aligned with P25; **`NOT_ATTEMPTED`** ⇒ **400** (no ZIP); final ZIP **≤** **2 MiB** else **413** (no partial body). HTTP **200** vs **404** matrix matches P25 (trace-id route **never** route-level **404** after valid body; conversation **404** only for inaccessible conversation scope).
- **P27 runtime trace replay batch core (implemented, internal-only):** synchronous, **sequential** orchestration over **up to 50** P18 **`replay`** calls per invocation. Single owner `RuntimeTraceReplayBatchService` under `application.service.runtime.tracereplaybatch` depends only on **`RuntimeTraceReplayService`** and **`RuntimeTraceQueryService`** (for **`listConversationTraceSummaries`** in **`BY_CONVERSATION`** mode). Selection modes mirror P24: **`BY_TRACE_IDS`** (ordered list, **≤50** raw ids before dedup, first-seen dedup) and **`BY_CONVERSATION`** (first **50** rows, **`page=0`**, **`createdAt DESC`** via P16 listing). Each item uses **`RuntimeTraceReplayRequest.byTraceId`** only. **`NotFoundException`** from **`listConversationTraceSummaries`** (conversation missing or inaccessible) **propagates** from the batch service — **not** mapped to **`EMPTY_SELECTION`** or batch **`NOT_ATTEMPTED`**; the HTTP adapter (**P28**) maps that exception to **404**. Per-trace **`NotFoundException`** from **`replay`** is **batch-local** (one item row, batch continues). **No** REST in P27, **no** persistence, **no** export, **no** `RuntimeTraceReplayComparisonBatchService` / **`tracecomparisonbatch`**, repository, `ProcessQueryService`, orchestrator, or **async**. Transient results live under **`domain.runtime.tracereplaybatch`** (batch outcome, per-item rows with capped scalars, summary counters only — **`requestedCount` / `selectedCount` on the batch **result**, not on the summary).
- **P28 runtime trace replay batch query surfaces (implemented):** **HTTP adapter only** — **two** **`POST`** routes under `rag.api.product-base-path`: `.../runtime-traces/replays/batch` for **`BY_TRACE_IDS`** (JSON body **`traceIds`** only) and `.../conversations/{conversationId}/runtime-traces/replays/batch` for **`BY_CONVERSATION`**. `RuntimeTraceReplayBatchController` depends on **`RuntimeTraceReplayBatchService` only** and calls **`execute`** only. Strict JSON request bodies via a dedicated **`ObjectMapper`** (**`FAIL_ON_UNKNOWN_PROPERTIES`**, **`JavaTimeModule`**); **`NOT_ATTEMPTED`** ⇒ **400** (never **200**); trace-id route **never** uses route-level **404** for per-trace access problems (they appear as item rows); conversation route may **404** only when conversation scope is missing/inaccessible (**`NotFoundException`**); empty conversation selection ⇒ **200** **`EMPTY_SELECTION`**. Bounded DTOs under **`interfaces.rest.dto.tracereplaybatch`** (static factories only; **no** mapper **`@Service`**); **no** export/ZIP.
- **P29 runtime trace replay batch export core (implemented):** **two** **`POST`** ZIP download routes only (`.../runtime-traces/replays/batch/export` and `.../conversations/{conversationId}/runtime-traces/replays/batch/export`). `RuntimeTraceReplayBatchExportController` depends on **`RuntimeTraceReplayBatchExportService` only** (never **`RuntimeTraceReplayBatchService`** directly). The export service calls **`RuntimeTraceReplayBatchService#execute` exactly once** per request and packages **`manifest.json`** (`exportKind: REPLAY_BATCH`, `schemaVersion` JSON integer **1**, `truncated: false`, `zipSizeBytes` fixed-point match) then **`batch.json`** — **only** **`RuntimeTraceReplayBatchResponseDto.fromBatchResult`** serialized (same shape as P28 **200**). P28 request DTOs reused; strict-schema **400** rules aligned with P28; **`NOT_ATTEMPTED`** ⇒ **400** (no ZIP); final ZIP **≤** **2 MiB** else **413** (no partial body). HTTP **200** vs **404** matrix matches P28 (trace-id route **never** route-level **404** after valid body; conversation **404** only for inaccessible conversation scope).
- `PromptStack` is populated via **Runtime Configuration** (`SystemPromptComposer`); see [configuration-resolution-model.md](configuration-resolution-model.md).
- `ExecutionTrace` is consumed by **Platform & Ops** (observability) and **Experimentation / Lab** for runs.

## Alignment with the repository (current state)

### What already exists

- `ProcessQueryService` and `service.query.pipeline` (preparation, routing, synthesis) — see [BACKEND_PACKAGES.md](BACKEND_PACKAGES.md).
- Retrievers, rankers (`FaithfulnessRanker`, `LLMAsJudgeRanker`), reasoning strategies, metadata tools (`com.uniovi.rag.tool`).
- `ToolExecutionContext` in code (tool layer context; **not** identical to target `ExecutionContext` but related).
- Streaming / SSE product flows per [integration-flows.md](integration-flows.md).

### What is partial

- Named target components **map unevenly** to classes: several judges/rankers exist; **clarification session**, **adaptive routing engine**, and **execution trace** as unified concepts may be implicit or fragmented.
- **Workflow** selection may be spread across flags, presets, and services rather than one `WorkflowSelector` abstraction.
- **S0–S4** scenario ladder not uniformly encoded as `ExecutionRoute` values.

### S0–S1 runtime engine base

- **System base**: all implemented based on what had been developed before.
- **Implemented in code:** `com.uniovi.rag.domain.runtime.engine.ExecutionContext` (factory-built only via `ExecutionContextFactory`), `RagExecutionOrchestrator`, `WorkflowSelector` (deterministic matrix from `RagConfig` + `MaterializationStrategy`), five `ExecutionWorkflow` beans (`DirectLlmWorkflow`, `FullCorpusWorkflow`, `DocumentDenseRagWorkflow`, `ChunkDenseRagWorkflow`, `ChunkDenseMetadataWorkflow`), `KnowledgeRuntimeSnapshotSelector`, `SnapshotCorpusAssembler`, `AdvancedRetrievalPipeline` (snapshot-bound retrieval for the three dense workflows), `RagExecutionResult` / `ExecutionTrace`.
- **Product path:** `ProcessQueryService` is a façade: `ExecutionContextFactory` → `RagExecutionOrchestrator` → map to `QueryResponse`. Live resolution uses `ConfigResolverService.resolve` via `RuntimeConfigResolutionService.resolveForOrchestratedExecute` with the same merged conversation JSON as `ChatScopedRagConfigResolver`.
- **Errors:** `unsupported-runtime-configuration` and `knowledge-snapshot-unavailable` surface as `RagServiceException` with HTTP **422** (see `ErrorCode`).

### S2: P8 advanced retrieval core

- **Implemented in code:** `AdvancedRetrievalPipeline` (`com.uniovi.rag.application.service.runtime.retrieval`) is the **only** retrieval entrypoint for `DocumentDenseRagWorkflow`, `ChunkDenseRagWorkflow`, and `ChunkDenseMetadataWorkflow`. It consumes `ExecutionContext`, `QueryPlan`, and workflow name; uses `QueryPlan.rewrittenQueryText` as the canonical retrieval query; supports `DENSE_ONLY` and `HYBRID_DENSE_SPARSE` (dense + PostgreSQL FTS, RRF-only fusion); applies deterministic rerank, filter, and extractive compression; returns `CuratedContextSet` with `RetrievalDiagnostics` and frozen substage traces (`retrieval_build_request`, `retrieval_dense`, `retrieval_sparse`, `retrieval_fuse`, `retrieval_rerank`, `retrieval_filter`, `retrieval_compress`). `RagExecutionOrchestrator` merges those traces and copies diagnostics into `ExecutionTrace.retrievalDiagnostics`.
- **Frozen constraint:** `WorkflowSelector` remains the only workflow chooser; `MaterializationStrategy.HYBRID` with retrieval routes like chunk-level retrieval (metadata flag selects chunk vs chunk+metadata workflow); `STRUCTURED_SEARCH` with retrieval remains unsupported on the orchestrated path.

### What is still missing

- **Persisted** `ExecutionTrace` / full lab–product parity for trace export beyond in-memory + logs; STRUCTURED_SEARCH on the orchestrated path; phases beyond P10 (clarification execution, memory advisor, routing advisor, judges as traceable stages).
- Full **ToolPolicy** surface matching target semantics.
- All **four judges** as discrete, traceable stages if the target requires them for S4.
- Documentation-to-code traceability matrix (optional future table) without changing this canonical vocabulary.

### S2: P5–P6 query understanding core

- **Implemented in code:** `QueryUnderstandingPipeline` as a mandatory runtime stage that produces an immutable `QueryPlan` for every orchestrated request and is executed by `RagExecutionOrchestrator` **before** `WorkflowSelector`. QU failures are non-fatal and visible in `QueryPlan.pipelineNotes` and `ExecutionTrace` stage entries.
- **Frozen constraint:** `WorkflowSelector` does not branch on `QueryPlan`; `QueryPlan` is planning metadata only.

### S2: P7 deterministic tools core

- **Implemented in code:** `DeterministicToolStrategy` (package `application.service.runtime.tool`) is the **only** deterministic tool entrypoint. `RagExecutionOrchestrator` runs it **after** `WorkflowSelector.select(...)` and **before** `workflow.execute(...)`. Resolution uses **only** `ExecutionContext`, `ResolvedRuntimeConfig`, `QueryPlan`, and the selected workflow name; it does not re-run classifier/NER or reconstruct `QueryPlan`. When ambiguity is not `SUFFICIENT` or `toolsEnabled` is false, tools are suppressed and the trace records the reason; when a tool executes successfully, the orchestrator short-circuits and returns the tool answer without invoking the workflow. `ExecutionTrace` includes `tool_resolve`, `tool_execute`, `tool_result_map` stages plus summary fields (`deterministicToolOutcome`, `deterministicToolKind`, `deterministicToolDetail`).
- **Frozen constraint:** `ExecutionWorkflow` implementations do not perform tool resolution, selection, or execution.
